# Notas:

Primer programa de repaso de node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     La base para la tabla de multiplicar                   [number]
  -l, --listar   Lista los archivos que lleguen                        [boolean]
  -h, --hasta    Limite de multiplicación                               [number]
```
